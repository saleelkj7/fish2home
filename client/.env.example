# Copy this to .env for local dev, or set VITE_API_URL in Vercel's
# project settings for production.
#
# Local dev (matches server running on :5000):
VITE_API_URL=http://localhost:5000

# Production (after your Render backend is live):
# VITE_API_URL=https://api.fishtokri.co.in
